/**
 * @private
 */
/*
IonicModule.config([
  '$provide',
function($provide) {
  function $AnimateDecorator() {

    return {
      step: function(element, parentElement, afterElement, doneCallback, amount) {
      }
    }
  }

  $provide.decorator('$animate', ['$delegate', '$timeout', $AnimateDecorator]);
}]);
*/
